#!/bin/bash
DIR=`dirname $0`

# Backuping
rename 's/$/.bak/' ~/.bash*

# Copying
cp $DIR/.bash* ~/
cat $DIR/.profile >> ~/.profile
cat $DIR/.inputrc >> ~/.inputrc
cp $DIR/.tmux.conf ~/
cp $DIR/.gitconfig ~/
cp -r $DIR/.vim ~/
cp -r $DIR/.terminfo ~/

# Setuping
~/.vim/setup
